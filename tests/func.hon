fn add(first: int, second: int) {
    return first + second;
}

// let result = add(1, 2);
// @println("Result: ", result);

// @set_stack_logging(true);
fn scope_test(a: number, b: number) {
    return if (a > b) [b, a, a + b] else [a, b, a + b];
}

@println("`scope_test` Result: ", scope_test(4, 2));
