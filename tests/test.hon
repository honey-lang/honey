fn recursive_countdown(iterations: int) {
    if (iterations <= 0) {
        @println("Blastoff!");
        return;
    }
    @println(iterations, "...");
    recursive_countdown(iterations - 1);
}

recursive_countdown(3);
